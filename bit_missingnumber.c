#include<stdio.h>
int missing(int arr[],n)
{
    
}
int main()
{
    int arr[20],n,k;
    printf("enter an element range ");
    scanf("%d",&n);
    printf(" enter the elements \n");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    missing(arr,n);
}